FROM alpine:3.21

RUN adduser -D mango
WORKDIR /home/mango

COPY ./dist/mango /usr/local/bin/mango

USER mango

HEALTHCHECK --interval=30s --timeout=10s --start-period=15s --retries=3 \
  CMD wget -qO- http://localhost:9000/ > /dev/null || exit 1

EXPOSE 9000

CMD ["/usr/local/bin/mango"]
